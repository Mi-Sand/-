# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# --- Виртуальное окружение ----------------------------------------------------
venv/
env/
ENV/
.venv/

# --- Django ------------------------------------------------------------------
db.sqlite3
db.sqlite3-journal
# Копии базы и выгрузки фотографий, снятые вручную рядом с проектом.
# Без этих строк файл вида backup_2026-08-06.sqlite3 попадёт в репозиторий
# при обычном `git add -A` — вместе со всеми данными склада.
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
media_*.zip
/staticfiles/
/media/
*.log

# --- Переменные окружения ------------------------------------------------------
.env

# --- IDE -----------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo

# --- Windows-специфичные файлы ---------------------------------------------
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# --- macOS-специфичные файлы -------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride

# --- Linux-специфичные файлы --------------------------------------------------
*~
.directory

# --- Docker --------------------------------------------------------------------
.docker/

# --- Резервные копии ------------------------------------------------------------
# Сюда update.ps1 складывает копии базы, фотографий и .env перед
# обновлением. В репозиторий им нельзя: это боевые данные и пароли.
backups/

# --- Тестирование и покрытие --------------------------------------------------
.coverage
htmlcov/
.pytest_cache/
